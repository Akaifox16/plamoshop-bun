{
  "name": "plamoshop-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.1.37",
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
